const socket=io();
const state={myId:null,myName:'',roomCode:'',hostId:null,players:[],isHost:false,tool:'pen',color:'#000000',brushSize:8,drawing:false,lastX:0,lastY:0,submitted:false,scores:{},gameMode:'draw'};
const hist={stack:[],idx:-1,max:50};
const gHist={stack:[],idx:-1,max:50};
const S={login:document.getElementById('screen-login'),lobby:document.getElementById('screen-lobby'),drawing:document.getElementById('screen-drawing'),guess:document.getElementById('screen-guess'),results:document.getElementById('screen-results'),guessOver:document.getElementById('screen-guess-over')};
function show(n){Object.values(S).forEach(s=>s.classList.remove('active'));S[n].classList.add('active');}
function toast(msg,t=''){const c=document.getElementById('toast-container');const el=document.createElement('div');el.className='toast'+(t?' '+t:'');el.textContent=msg;c.appendChild(el);setTimeout(()=>el.remove(),3500);}
let colorWheelTarget='main';
let wheelDragging=false;
function pickFromWheel(wc,wcx,clientX,clientY){const rect=wc.getBoundingClientRect();const size=wc.width;const x=Math.round((clientX-rect.left)*(size/rect.width));const y=Math.round((clientY-rect.top)*(size/rect.height));if(x<0||y<0||x>=size||y>=size)return null;const px=wcx.getImageData(x,y,1,1).data;if(px[3]<10)return null;return'#'+[px[0],px[1],px[2]].map(v=>v.toString(16).padStart(2,'0')).join('');}
function applyWheelColor(hex){if(!hex)return;document.getElementById('wheel-preview').style.background=hex;if(colorWheelTarget==='main')setColor(hex);else setGColor(hex);}
function buildColorWheel(){const wc=document.getElementById('wheel-canvas');const size=220;wc.width=size;wc.height=size;const wcx=wc.getContext('2d');const cx=size/2,cy=size/2,r=size/2-2;for(let a=0;a<360;a++){const s=(a-1)*Math.PI/180,e=(a+1)*Math.PI/180;const g=wcx.createRadialGradient(cx,cy,0,cx,cy,r);g.addColorStop(0,'white');g.addColorStop(0.5,`hsl(${a},100%,50%)`);g.addColorStop(1,'black');wcx.beginPath();wcx.moveTo(cx,cy);wcx.arc(cx,cy,r,s,e);wcx.closePath();wcx.fillStyle=g;wcx.fill();}
wc.onmousedown=(e)=>{wheelDragging=true;applyWheelColor(pickFromWheel(wc,wcx,e.clientX,e.clientY));};
wc.onmousemove=(e)=>{if(!wheelDragging)return;applyWheelColor(pickFromWheel(wc,wcx,e.clientX,e.clientY));};
wc.onmouseup=()=>wheelDragging=false;
wc.ontouchstart=(e)=>{e.preventDefault();wheelDragging=true;const t=e.touches[0];applyWheelColor(pickFromWheel(wc,wcx,t.clientX,t.clientY));};
wc.ontouchmove=(e)=>{e.preventDefault();if(!wheelDragging)return;const t=e.touches[0];applyWheelColor(pickFromWheel(wc,wcx,t.clientX,t.clientY));};
wc.ontouchend=()=>wheelDragging=false;}
function openWheel(target){colorWheelTarget=target;buildColorWheel();document.getElementById('color-wheel-modal').classList.remove('hidden');}
document.getElementById('btn-open-wheel').addEventListener('click',()=>openWheel('main'));
document.getElementById('gbtn-open-wheel').addEventListener('click',()=>openWheel('guess'));
document.getElementById('wheel-close').addEventListener('click',()=>{document.getElementById('color-wheel-modal').classList.add('hidden');wheelDragging=false;});
const PAL=['#000000','#FFFFFF','#94a3b8','#475569','#ef4444','#f97316','#f59e0b','#eab308','#22c55e','#10b981','#06b6d4','#3b82f6','#8b5cf6','#a855f7','#ec4899','#f43f5e','#84cc16','#14b8a6','#6366f1','#0ea5e9','#fde68a','#bbf7d0','#bfdbfe','#fecaca','#7f1d1d','#1e3a5f','#14532d','#312e81','#78350f','#1e1b4b'];
function buildPalette(id,onPick,activeColor){const c=document.getElementById(id);c.innerHTML='';PAL.forEach(h=>{const s=document.createElement('div');s.className='swatch';s.style.background=h;s.title=h;if(h===activeColor)s.classList.add('on');s.addEventListener('click',()=>onPick(h));c.appendChild(s);});}
function setColor(h){state.color=h;document.getElementById('color-preview').style.background=h;document.querySelectorAll('#color-palette .swatch').forEach(s=>s.classList.toggle('on',s.title===h));if(state.tool==='eraser'||state.tool==='eyedropper')setTool('pen');}
function setGColor(h){state.color=h;document.getElementById('gcolor-preview').style.background=h;document.querySelectorAll('#gcolor-palette .swatch').forEach(s=>s.classList.toggle('on',s.title===h));if(state.tool==='eraser'||state.tool==='eyedropper')setTool('pen');}
function saveH(h,cv,ct){h.stack=h.stack.slice(0,h.idx+1);h.stack.push(ct.getImageData(0,0,cv.width,cv.height));if(h.stack.length>h.max)h.stack.shift();h.idx=h.stack.length-1;updUndoBtns();}
function undoH(h,cv,ct){if(h.idx<=0)return;h.idx--;ct.putImageData(h.stack[h.idx],0,0);updUndoBtns();}
function redoH(h,cv,ct){if(h.idx>=h.stack.length-1)return;h.idx++;ct.putImageData(h.stack[h.idx],0,0);updUndoBtns();}
function updUndoBtns(){document.getElementById('btn-undo').disabled=hist.idx<=0;document.getElementById('btn-redo').disabled=hist.idx>=hist.stack.length-1;document.getElementById('gbtn-undo').disabled=gHist.idx<=0;document.getElementById('gbtn-redo').disabled=gHist.idx>=gHist.stack.length-1;}
const canvas=document.getElementById('drawing-canvas');
const ctx=canvas.getContext('2d',{willReadFrequently:true});
function resizeCanvas(){const header=document.querySelector('#screen-drawing .draw-header');const toolbar=document.querySelector('#screen-drawing .toolbar');const hh=header?header.offsetHeight:50;const th=toolbar?toolbar.offsetHeight:80;const avail=window.innerHeight-hh-th-24;const sz=Math.min(window.innerWidth-24,avail,720);canvas.width=sz;canvas.height=sz;ctx.fillStyle='#fff';ctx.fillRect(0,0,sz,sz);hist.stack=[];hist.idx=-1;saveH(hist,canvas,ctx);}
function gpos(e,cv){const r=cv.getBoundingClientRect();const sx=cv.width/r.width,sy=cv.height/r.height;const cx=e.touches?e.touches[0].clientX:e.clientX,cy=e.touches?e.touches[0].clientY:e.clientY;return{x:(cx-r.left)*sx,y:(cy-r.top)*sy};}
function pickColor(ct,x,y){const px=ct.getImageData(Math.round(x),Math.round(y),1,1).data;return'#'+[px[0],px[1],px[2]].map(v=>v.toString(16).padStart(2,'0')).join('');}
function sdown(e){if(state.submitted)return;e.preventDefault();state.drawing=true;const{x,y}=gpos(e,canvas);if(state.tool==='eyedropper'){setColor(pickColor(ctx,x,y));setTool('pen');state.drawing=false;return;}if(state.tool==='fill'){fill(Math.round(x),Math.round(y),state.color,canvas,ctx);saveH(hist,canvas,ctx);state.drawing=false;return;}state.lastX=x;state.lastY=y;ctx.beginPath();ctx.arc(x,y,state.brushSize/2,0,Math.PI*2);ctx.fillStyle=state.tool==='eraser'?'#fff':state.color;ctx.fill();}
function smove(e){if(!state.drawing||state.submitted||state.tool==='fill'||state.tool==='eyedropper')return;e.preventDefault();const{x,y}=gpos(e,canvas);ctx.beginPath();ctx.moveTo(state.lastX,state.lastY);ctx.lineTo(x,y);ctx.strokeStyle=state.tool==='eraser'?'#fff':state.color;ctx.lineWidth=state.brushSize;ctx.lineCap='round';ctx.lineJoin='round';ctx.stroke();state.lastX=x;state.lastY=y;}
function send(e){if(state.drawing&&state.tool!=='fill'&&state.tool!=='eyedropper')saveH(hist,canvas,ctx);state.drawing=false;}
canvas.addEventListener('mousedown',sdown);canvas.addEventListener('mousemove',smove);canvas.addEventListener('mouseup',send);canvas.addEventListener('mouseleave',send);canvas.addEventListener('touchstart',sdown,{passive:false});canvas.addEventListener('touchmove',smove,{passive:false});canvas.addEventListener('touchend',send);
const gcanvas=document.getElementById('guess-canvas');
const gctx=gcanvas.getContext('2d',{willReadFrequently:true});
let isDrawer=false,glastX=0,glastY=0,gdrawing=false,gCanvasReady=false;
function resizeGCanvas(cb){const header=document.querySelector('#screen-guess .draw-header');const toolbar=document.querySelector('#screen-guess .toolbar');const hh=header?header.offsetHeight:50;const th=toolbar?toolbar.offsetHeight:80;const avail=window.innerHeight-hh-th-24;const sz=Math.min(window.innerWidth*0.65-24,avail,680);gcanvas.width=sz;gcanvas.height=sz;gctx.fillStyle='#fff';gctx.fillRect(0,0,sz,sz);gHist.stack=[];gHist.idx=-1;saveH(gHist,gcanvas,gctx);gCanvasReady=true;if(cb)cb();}
function replayStroke(s){const sx=gcanvas.width/s.w,sy=gcanvas.height/s.h;if(s.type==='dot'){gctx.beginPath();gctx.arc(s.x*sx,s.y*sy,s.size/2,0,Math.PI*2);gctx.fillStyle=s.color;gctx.fill();}else if(s.type==='line'){gctx.beginPath();gctx.moveTo(s.x1*sx,s.y1*sy);gctx.lineTo(s.x2*sx,s.y2*sy);gctx.strokeStyle=s.color;gctx.lineWidth=s.size;gctx.lineCap='round';gctx.lineJoin='round';gctx.stroke();}else if(s.type==='fill'){fill(Math.round(s.x*sx),Math.round(s.y*sy),s.color,gcanvas,gctx);}}
function gsdown(e){if(!isDrawer)return;e.preventDefault();gdrawing=true;const{x,y}=gpos(e,gcanvas);if(state.tool==='eyedropper'){setGColor(pickColor(gctx,x,y));setTool('pen');gdrawing=false;return;}if(state.tool==='fill'){fill(Math.round(x),Math.round(y),state.color,gcanvas,gctx);saveH(gHist,gcanvas,gctx);socket.emit('guess:stroke',{type:'fill',x:Math.round(x),y:Math.round(y),color:state.color,w:gcanvas.width,h:gcanvas.height});gdrawing=false;return;}glastX=x;glastY=y;gctx.beginPath();gctx.arc(x,y,state.brushSize/2,0,Math.PI*2);gctx.fillStyle=state.tool==='eraser'?'#fff':state.color;gctx.fill();socket.emit('guess:stroke',{type:'dot',x,y,size:state.brushSize,color:state.tool==='eraser'?'#fff':state.color,w:gcanvas.width,h:gcanvas.height});}
function gsmove(e){if(!gdrawing||!isDrawer||state.tool==='fill'||state.tool==='eyedropper')return;e.preventDefault();const{x,y}=gpos(e,gcanvas);gctx.beginPath();gctx.moveTo(glastX,glastY);gctx.lineTo(x,y);gctx.strokeStyle=state.tool==='eraser'?'#fff':state.color;gctx.lineWidth=state.brushSize;gctx.lineCap='round';gctx.lineJoin='round';gctx.stroke();socket.emit('guess:stroke',{type:'line',x1:glastX,y1:glastY,x2:x,y2:y,size:state.brushSize,color:state.tool==='eraser'?'#fff':state.color,w:gcanvas.width,h:gcanvas.height});glastX=x;glastY=y;}
function gsend(){if(gdrawing&&state.tool!=='fill'&&state.tool!=='eyedropper')saveH(gHist,gcanvas,gctx);gdrawing=false;}
gcanvas.addEventListener('mousedown',gsdown);gcanvas.addEventListener('mousemove',gsmove);gcanvas.addEventListener('mouseup',gsend);gcanvas.addEventListener('mouseleave',gsend);gcanvas.addEventListener('touchstart',gsdown,{passive:false});gcanvas.addEventListener('touchmove',gsmove,{passive:false});gcanvas.addEventListener('touchend',gsend);
socket.on('guess:stroke',({stroke})=>{if(!gCanvasReady)return;replayStroke(stroke);});
socket.on('guess:strokeHistory',({strokes})=>{strokes.forEach(s=>replayStroke(s));});
socket.on('guess:clearCanvas',()=>{gctx.fillStyle='#fff';gctx.fillRect(0,0,gcanvas.width,gcanvas.height);gHist.stack=[];gHist.idx=-1;saveH(gHist,gcanvas,gctx);});
function h2a(h){return[parseInt(h.slice(1,3),16),parseInt(h.slice(3,5),16),parseInt(h.slice(5,7),16),255];}
function cmatch(a,b,t=16){return Math.abs(a[0]-b[0])<=t&&Math.abs(a[1]-b[1])<=t&&Math.abs(a[2]-b[2])<=t&&Math.abs(a[3]-b[3])<=t;}
function fill(sx,sy,hex,cv,ct){const w=cv.width,h=cv.height,id=ct.getImageData(0,0,w,h),d=id.data;const gi=(x,y)=>{const i=(y*w+x)*4;return[d[i],d[i+1],d[i+2],d[i+3]];};const tg=gi(sx,sy),fl=h2a(hex);if(cmatch(tg,fl,4))return;const vis=new Uint8Array(w*h),q=[sx+sy*w];vis[sx+sy*w]=1;while(q.length){const p=q.pop(),x=p%w,y=(p/w)|0,i=p*4;d[i]=fl[0];d[i+1]=fl[1];d[i+2]=fl[2];d[i+3]=fl[3];for(const n of[x>0?p-1:-1,x<w-1?p+1:-1,y>0?p-w:-1,y<h-1?p+w:-1]){if(n<0||vis[n])continue;if(cmatch(gi(n%w,(n/w)|0),tg)){vis[n]=1;q.push(n);}}}ct.putImageData(id,0,0);}
function setTool(t){state.tool=t;document.querySelectorAll('.tbtn').forEach(b=>b.classList.remove('active'));document.getElementById('tool-'+t)?.classList.add('active');document.getElementById('gtool-'+t)?.classList.add('active');canvas.style.cursor=t==='fill'?'cell':'crosshair';gcanvas.style.cursor=t==='fill'?'cell':'crosshair';}
document.getElementById('tool-pen').addEventListener('click',()=>setTool('pen'));
document.getElementById('tool-eraser').addEventListener('click',()=>setTool('eraser'));
document.getElementById('tool-fill').addEventListener('click',()=>setTool('fill'));
document.getElementById('tool-eyedropper').addEventListener('click',()=>setTool('eyedropper'));
document.getElementById('gtool-pen').addEventListener('click',()=>setTool('pen'));
document.getElementById('gtool-eraser').addEventListener('click',()=>setTool('eraser'));
document.getElementById('gtool-fill').addEventListener('click',()=>setTool('fill'));
document.getElementById('gtool-eyedropper').addEventListener('click',()=>setTool('eyedropper'));
document.getElementById('btn-undo').addEventListener('click',()=>undoH(hist,canvas,ctx));
document.getElementById('btn-redo').addEventListener('click',()=>redoH(hist,canvas,ctx));
document.getElementById('gbtn-undo').addEventListener('click',()=>undoH(gHist,gcanvas,gctx));
document.getElementById('gbtn-redo').addEventListener('click',()=>redoH(gHist,gcanvas,gctx));
document.addEventListener('keydown',(e)=>{const inInput=e.target.tagName==='INPUT';if(inInput)return;if(e.ctrlKey&&e.key==='z'&&!e.shiftKey){e.preventDefault();S.drawing.classList.contains('active')?undoH(hist,canvas,ctx):undoH(gHist,gcanvas,gctx);}if(e.ctrlKey&&(e.key==='y'||(e.key==='z'&&e.shiftKey))){e.preventDefault();S.drawing.classList.contains('active')?redoH(hist,canvas,ctx):redoH(gHist,gcanvas,gctx);}});
const slider=document.getElementById('size-slider');slider.addEventListener('input',()=>{state.brushSize=+slider.value;document.getElementById('size-val').textContent=slider.value;});
const gslider=document.getElementById('gsize-slider');gslider.addEventListener('input',()=>{state.brushSize=+gslider.value;document.getElementById('gsize-val').textContent=gslider.value;});
document.getElementById('btn-clear').addEventListener('click',()=>{const b=document.getElementById('btn-clear');b.style.background='rgba(239,68,68,0.25)';setTimeout(()=>{b.style.background='';ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);saveH(hist,canvas,ctx);},400);});
document.getElementById('gbtn-clear').addEventListener('click',()=>{gctx.fillStyle='#fff';gctx.fillRect(0,0,gcanvas.width,gcanvas.height);saveH(gHist,gcanvas,gctx);socket.emit('guess:clear');});
document.getElementById('btn-submit').addEventListener('click',()=>{if(state.submitted)return;state.submitted=true;socket.emit('game:submitDrawing',{drawing:canvas.toDataURL('image/png')});document.getElementById('sub-overlay').classList.remove('hidden');document.getElementById('btn-submit').disabled=true;});
const iname=document.getElementById('input-name');
const icode=document.getElementById('input-code');
let selectedMode='draw';
document.querySelectorAll('.mode-opt').forEach(el=>{el.addEventListener('click',()=>{document.querySelectorAll('.mode-opt').forEach(o=>o.classList.remove('active'));el.classList.add('active');selectedMode=el.dataset.mode;});});
document.getElementById('btn-create').addEventListener('click',()=>{const n=iname.value.trim();if(n.length<2){toast('Apelido precisa ter pelo menos 2 letras!','err');iname.focus();return;}state.myName=n;socket.emit('room:create',{name:n,mode:selectedMode});});
document.getElementById('btn-join').addEventListener('click',()=>{const n=iname.value.trim(),c=icode.value.trim();if(n.length<2){toast('Apelido precisa ter pelo menos 2 letras!','err');iname.focus();return;}if(c.length<6){toast('Código tem 6 caracteres!','err');icode.focus();return;}state.myName=n;socket.emit('room:join',{name:n,code:c});});
icode.addEventListener('input',()=>{icode.value=icode.value.toUpperCase().replace(/[^A-Z0-9]/g,'');});
function loadRooms(){socket.emit('rooms:list');}
document.getElementById('btn-refresh').addEventListener('click',loadRooms);
socket.on('rooms:data',({rooms})=>{const l=document.getElementById('rooms-list');if(!rooms||!rooms.length){l.innerHTML='<p class="empty">Nenhuma sala aberta.</p>';return;}l.innerHTML='';rooms.forEach(r=>{const el=document.createElement('div');el.className='room-entry';el.innerHTML=`<div><div class="re-code">${r.code}</div><div class="re-count">${r.count}/10 · <span class="re-mode">${r.mode==='guess'?'🔍 Adivinhe':'🎨 Livre'}</span></div></div><button class="re-btn">Entrar</button>`;el.querySelector('button').addEventListener('click',()=>{icode.value=r.code;toast('Código preenchido!');});l.appendChild(el);});});
const AVCOLS=['#7c3aed','#06b6d4','#f59e0b','#22c55e','#ef4444','#ec4899','#8b5cf6','#14b8a6','#f97316','#3b82f6'];
function avcol(n){let h=0;for(let i=0;i<n.length;i++)h=n.charCodeAt(i)+((h<<5)-h);return AVCOLS[Math.abs(h)%AVCOLS.length];}
function renderLobby(){const g=document.getElementById('players-grid');g.innerHTML='';state.players.forEach(p=>{const c=document.createElement('div');c.className='pcard';if(p.id===state.hostId)c.classList.add('host');if(p.id===state.myId)c.classList.add('you');let badge='';if(p.id===state.hostId&&p.id===state.myId)badge='<span class="pbadge bhost">HOST 👑 você</span>';else if(p.id===state.hostId)badge='<span class="pbadge bhost">HOST 👑</span>';else if(p.id===state.myId)badge='<span class="pbadge byou">você</span>';c.innerHTML=`<div class="pavatar" style="background:${avcol(p.name)}">${p.name[0].toUpperCase()}</div><span class="pname">${p.name}</span>${badge}`;g.appendChild(c);});document.getElementById('player-count').textContent=state.players.length;const bs=document.getElementById('btn-start'),wm=document.getElementById('wait-msg'),mh=document.getElementById('min-hint');if(state.isHost){bs.classList.remove('hidden');wm.classList.add('hidden');bs.disabled=state.players.length<2;mh.style.display=state.players.length<2?'block':'none';}else{bs.classList.add('hidden');wm.classList.remove('hidden');mh.style.display='none';}}
document.getElementById('btn-copy').addEventListener('click',()=>{navigator.clipboard.writeText(state.roomCode).then(()=>toast('Código copiado! 📋','ok')).catch(()=>toast('Não foi possível copiar.','err'));});
document.getElementById('btn-start').addEventListener('click',()=>socket.emit('game:start'));
function updTimer(id,valId,t){const el=document.getElementById(valId),box=document.getElementById(id);el.textContent=t;el.classList.remove('y','r');box.classList.remove('tgreen','tyellow','tred');if(t>60)box.classList.add('tgreen');else if(t>30){el.classList.add('y');box.classList.add('tyellow');}else{el.classList.add('r');box.classList.add('tred');}}
function addChat(name,msg,type='normal'){const c=document.getElementById('chat-messages');const el=document.createElement('div');el.className='chat-msg'+(type==='correct'?' correct':type==='system'?' system':type==='me'?' me':'');if(type==='system'){el.innerHTML=`<span>${msg}</span>`;}else if(type==='me'){el.innerHTML=`<strong>Você:</strong> ${msg}`;}else{el.innerHTML=`<strong>${name}:</strong> ${msg}`;}c.appendChild(el);c.scrollTop=c.scrollHeight;}
function showResults({drawings,winnerId,winnerName,verdict}){show('results');document.getElementById('robo-wrap').style.display='flex';document.getElementById('res-content').classList.add('hidden');const bar=document.getElementById('robo-prog');let p=0;const iv=setInterval(()=>{p+=1.2;bar.style.width=Math.min(p,100)+'%';if(p>=100)clearInterval(iv);},30);setTimeout(()=>{document.getElementById('robo-wrap').style.display='none';document.getElementById('res-content').classList.remove('hidden');document.getElementById('winner-name').textContent=winnerName;document.getElementById('winner-verdict').textContent='"'+verdict+'"';const gal=document.getElementById('gallery');gal.innerHTML='';drawings.forEach(d=>{const card=document.createElement('div');card.className='dcard';if(d.id===winnerId)card.classList.add('win');const cv=document.createElement('canvas');cv.width=200;cv.height=200;const cx=cv.getContext('2d');cx.fillStyle='#fff';cx.fillRect(0,0,200,200);const ft=document.createElement('div');ft.className='dfoot';const au=document.createElement('span');au.className='dauthor';au.textContent=d.name;ft.appendChild(au);const pts=state.scores[d.id]||0;const bd=document.createElement('span');bd.className=d.id===winnerId?'dbadge-win':'dbadge';bd.textContent=(d.id===winnerId?'🏆 ':'')+pts+'pt';ft.appendChild(bd);card.appendChild(cv);card.appendChild(ft);gal.appendChild(card);if(d.drawing){const img=new Image();img.onload=()=>{cx.clearRect(0,0,200,200);cx.fillStyle='#fff';cx.fillRect(0,0,200,200);cx.drawImage(img,0,0,200,200);};img.src=d.drawing;}});const bn=document.getElementById('btn-next'),nw=document.getElementById('next-wait');if(state.isHost){bn.classList.remove('hidden');nw.classList.add('hidden');}else{bn.classList.add('hidden');nw.classList.remove('hidden');}},3200);}
document.getElementById('btn-next').addEventListener('click',()=>socket.emit('game:nextRound'));
document.getElementById('btn-guess-next').addEventListener('click',()=>socket.emit('game:nextRound'));
socket.on('connect',()=>{state.myId=socket.id;loadRooms();});
socket.on('room:created',({code,players,hostId,mode})=>{state.roomCode=code;state.players=players;state.hostId=hostId;state.isHost=true;state.gameMode=mode;document.getElementById('display-code').textContent=code;document.getElementById('lobby-mode-tag').textContent=mode==='guess'?'🔍 Adivinhe!':'🎨 Desenho Livre';renderLobby();show('lobby');});
socket.on('room:joined',({code,players,hostId,mode})=>{state.roomCode=code;state.players=players;state.hostId=hostId;state.isHost=socket.id===hostId;state.gameMode=mode;document.getElementById('display-code').textContent=code;document.getElementById('lobby-mode-tag').textContent=mode==='guess'?'🔍 Adivinhe!':'🎨 Desenho Livre';renderLobby();show('lobby');});
socket.on('room:playerJoined',({players,newPlayer})=>{state.players=players;renderLobby();toast(newPlayer+' entrou 👋');});
socket.on('room:playerLeft',({players,leftPlayer})=>{state.players=players;renderLobby();toast(leftPlayer+' saiu');});
socket.on('room:hostTransferred',({newHostId,newHostName})=>{state.hostId=newHostId;state.isHost=socket.id===newHostId;renderLobby();toast(newHostName+' agora é o host 👑');});
socket.on('room:error',({message})=>toast(message,'err'));
socket.on('game:started',({theme})=>{state.submitted=false;resizeCanvas();document.getElementById('sub-overlay').classList.add('hidden');document.getElementById('btn-submit').disabled=false;document.getElementById('current-theme').textContent=theme;buildPalette('color-palette',setColor,state.color);setTool('pen');setColor('#000000');slider.value=8;state.brushSize=8;document.getElementById('size-val').textContent='8';updTimer('timer-box','timer-val',120);show('drawing');});
socket.on('game:timerUpdate',({time})=>{if(S.drawing.classList.contains('active'))updTimer('timer-box','timer-val',time);else updTimer('guess-timer-box','guess-timer-val',time);});
socket.on('game:showResults',(data)=>{data.drawings.forEach(d=>{if(!state.scores[d.id])state.scores[d.id]=d.score||0;});if(!state.scores[data.winnerId])state.scores[data.winnerId]=0;state.scores[data.winnerId]++;showResults(data);});
socket.on('guess:yourTurn',({word,timeLeft})=>{isDrawer=true;gCanvasReady=false;show('guess');document.getElementById('guess-toolbar').classList.remove('hidden');document.getElementById('guess-input-wrap').classList.add('hidden');document.getElementById('guess-role-label').textContent='Desenhe:';document.getElementById('guess-word-display').textContent=word;document.getElementById('chat-messages').innerHTML='';buildPalette('gcolor-palette',setGColor,state.color);setTool('pen');setColor('#000000');gslider.value=8;state.brushSize=8;document.getElementById('gsize-val').textContent='8';updTimer('guess-timer-box','guess-timer-val',timeLeft);document.getElementById('turn-end-overlay').classList.add('hidden');requestAnimationFrame(()=>resizeGCanvas());});
socket.on('guess:watchTurn',({drawerName,timeLeft})=>{isDrawer=false;gCanvasReady=false;show('guess');document.getElementById('guess-toolbar').classList.add('hidden');document.getElementById('guess-input-wrap').classList.remove('hidden');document.getElementById('guess-role-label').textContent='Desenhando:';document.getElementById('guess-word-display').textContent=drawerName;document.getElementById('chat-messages').innerHTML='';updTimer('guess-timer-box','guess-timer-val',timeLeft);document.getElementById('turn-end-overlay').classList.add('hidden');requestAnimationFrame(()=>resizeGCanvas(()=>{socket.emit('guess:requestHistory');}));});
socket.on('guess:chat',({name,message,isMe})=>{if(isMe){addChat(null,message,'me');}else{addChat(name,message);}});
socket.on('guess:correctAnswer',({name,guesserId})=>{const txt=guesserId===state.myId?'🎉 Você acertou!':'🎉 '+name+' acertou!';addChat('',txt,'correct');});
socket.on('guess:turnEnd',({word})=>{document.getElementById('turn-end-msg').textContent='A palavra era: '+word;document.getElementById('turn-end-overlay').classList.remove('hidden');});
socket.on('guess:gameOver',({players})=>{const sb=document.getElementById('guess-scoreboard');sb.innerHTML='';players.forEach((p,i)=>{const el=document.createElement('div');el.className='score-row'+(i===0?' first':'');el.innerHTML=`<span class="score-pos">${i===0?'🏆':i+1+'º'}</span><div class="pavatar" style="background:${avcol(p.name)};width:36px;height:36px;font-size:16px">${p.name[0].toUpperCase()}</div><span class="score-name">${p.name}</span><span class="score-pts">${p.score||0} pts</span>`;sb.appendChild(el);});const bn=document.getElementById('btn-guess-next'),nw=document.getElementById('guess-over-wait');if(state.isHost){bn.classList.remove('hidden');nw.classList.add('hidden');}else{bn.classList.add('hidden');nw.classList.remove('hidden');}show('guessOver');});
socket.on('disconnect',()=>toast('Conexão perdida. Recarregue.','err'));
function sendGuess(){const inp=document.getElementById('guess-input');const val=inp.value.trim();if(!val)return;socket.emit('guess:tryAnswer',{answer:val});inp.value='';}
document.getElementById('guess-send').addEventListener('click',sendGuess);
document.getElementById('guess-input').addEventListener('keydown',(e)=>{if(e.key==='Enter')sendGuess();});
window.addEventListener('resize',()=>{if(S.drawing.classList.contains('active'))resizeCanvas();if(S.guess.classList.contains('active'))resizeGCanvas();});
show('login');
